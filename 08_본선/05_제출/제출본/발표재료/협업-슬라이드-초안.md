---
tags:
  - area/submission
  - type/slide-draft
  - status/draft
date: 2026-07-05
up: "[[_05_제출_MOC]]"
---
# "AI 협업 방식" 슬라이드 초안

> 대외비 — 6/29 공식발표 전 비공개. 자원 인벤토리 금액은 팀 구두 확인값(볼트 미검증, "(팀 확인값)" 표기). 그 외 숫자는 레포·git·볼트 실측(명령 병기). 발표 전 재검증 필수.

---

## 1. 투입 자원 인벤토리 (핵심 — 사용자 지정)

| 자원 | 값 | 근거 |
|---|---|---|
| Claude API | **$450 전량 소진** | 팀 확인값 |
| Codex Pro | **$200** | 팀 확인값 |
| Claude Pro 5x 구독 | **2개** | 팀 확인값 |
| 로컬 모델 | Ollama + EXAONE 3.5(무료, 온프레미스) | `llm-runs.jsonl` 실행 기록·`_vendor/JB_project2` 코드 |
| 디자인 | Figma | 도구 레지스트리 등록(`08_본선/_system/tools/registry-plugins.md`) — 실사용 캡처는 미확인 |
| 문서·지식관리 | Obsidian + Syncthing(팀 볼트 동기화, 무료) | `.gitignore`의 `.stfolder`/`.obsidian` 제외 규칙 |
| 코드 협업 | GitHub(fork→PR, 무료) | `gh pr list` 실측(§3 보조 빅넘버) |

금액 3건은 팀이 구두로 확정해 준 값이므로 그대로 쓴다 — 볼트에 결제 로그가 없어 추가로 검증하거나 다른 금액을 추정해 넣지 않았다.

---

## 2. 슬라이드 레이아웃 제안 (자원 카드 + 실측 스트립)

```
┌──────────────────────────────────────────────────────────┐
│   사람 4명이 이 자원을 투입해 AI 팀을 지휘했다                 │  ← 헤드라인
├───────────┬───────────┬───────────┬───────────┬───────────┤
│ Claude API│ Codex Pro │ Claude Pro│  로컬모델   │  협업도구   │
│   $450    │   $200    │  5x ×2    │Ollama/EXAONE│Figma·Obsidian│
│  전량소진  │           │           │   (무료)   │·Syncthing·GitHub│
├───────────┴───────────┴───────────┴───────────┴───────────┤
│  25일 · 219커밋 · 문서 611개(본선 498) · 결정 51건 · 역할 14개 │ ← 실측 스트립 한 줄
├──────────────────────────────────────────────────────────┤
│  캡션: "구독형 AI 자원을 사람이 지휘해 만든 결과물"             │
└──────────────────────────────────────────────────────────┘
```

---

## 3. 보조 빅넘버 (기존 6개, 축소 유지)

| 숫자 | 라벨 | 산출 방법 |
|---|---|---|
| 25일 | 전체 기간(2026-06-11 첫 커밋 → 7-05) | `git log --reverse --format=%ad --date=short \| head -1` |
| 219건 | 총 커밋 수 | `git rev-list --count HEAD` |
| 611개 / 498개 | 볼트 전체 문서 / 본선 전용 문서 | `find . -name "*.md"` (node_modules·_vendor 제외) |
| 51건 | append-only 의사결정 로그 항목 | decision-log 현재본+아카이브 `### 날짜 · 결정` 카운트 |
| 14개 | AGENTS.md 협업 역할 수 | `08_본선/AGENTS.md` 역할표 |
| 15건 PR(14 merged) | GitHub fork→PR 협업 | `gh pr list --repo LSB-afk/JB-Fin-AI-Challenge --state all` |

참고로만 남기는 값(빅넘버 승격 안 함): 58종/14종(제품 내부 에이전트, 코드등록/조직도노출 — `08_본선/03_제품/reports/구현현황-JB_project2.md:179`), JB_project2 fork PR 3건, Playwright E2E 21+73개, 세션·프롬프트 로그 1,926줄. 지어내지 않고 제외한 것: 서브에이전트 누적 토큰(부분집계뿐), `llm-runs.jsonl` 케이스당 비용(런 4건, 파일럿), 전사 토큰 사용량(집계 없음).

---

## 4. 캡처 촬영 리스트 (자원 증빙 우선순위)

| 우선순위 | 캡처 대상 | 방식 | 증명하는 것 |
|---|---|---|---|
| 1 | Claude API / Codex Pro 사용 현황 | **화면 캡처 금지 — 금액은 슬라이드 텍스트로만 표기**(대시보드엔 결제수단·계정 정보 노출 위험) | "$450·$200 전량 소진"이 구두가 아니라 실제 소비임을 팀이 보증 |
| 2 | GitHub PR 목록(`LSB-afk/JB-Fin-AI-Challenge`, 15건) | 화면 캡처 가능(금액 없음) | fork→PR 협업 실이력 |
| 3 | Claude Code 병렬 서브에이전트 실행 화면 | 화면 캡처 가능 | AI 팀 동시 실행 구조 |
| 4 | Obsidian 그래프 뷰(08_본선 498개 노드) | 화면 캡처 가능 | 문서 규모·연결 구조 |
| 5 | `decision-log.md` 실제 파일(결정 1~2건 확대) | 화면 캡처 가능 | append-only 결정 규율 |
| 선택 | Figma 작업 화면 | 실사용 산출물 확인 후에만 | 미확인 상태라 지금은 후보에서 제외 권장 |

---

## 5. 한 줄 서사 후보

1. "Claude API $450, Codex Pro $200, Claude Pro 구독 2개 — 이 자원을 사람 4명이 지휘해 25일 만에 금융 AI 콘솔을 만들었다."
2. "AI 구독료를 쓴 게 아니라 AI 팀을 고용해 썼다 — 219번의 커밋과 51건의 의사결정이 그 증거다."
