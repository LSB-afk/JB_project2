---
tags: [area/product, type/ux, status/active]
date: 2026-07-04
up: "[[_쇼케이스-빌드플랜|쇼케이스 빌드플랜]]"
---
# F1 기업여신 딥 — 오태림(JBG-205) 실 LLM 흐름

> **[대외비]** 인물·거래는 전부 합성(PII無). 본 문서는 [[_쇼케이스-빌드플랜]] F1 행의 상세 설계도다. 화면 자체의 와이어프레임/컴포넌트/데이터바인딩은 재정의하지 않고 [[01-메인-업무보드]]·[[02-케이스-상세]]·[[03-에이전트-실행뷰]]·[[04-승인-대기·결정]]·[[05-통계-추적-패널]]을 링크로 참조한다. 이 문서가 새로 정의하는 것은 **오태림 케이스 하나가 그 화면들을 어떤 순서·데이터·실동작 등급으로 관통하는가**다.

## 1. 한 줄 시나리오 + 페르소나

**오태림, 39세, 전북 군산시, 산업용 센서 제조 스타트업 대표.** JB우리캐피탈(JBWC) 기업여신·기술신용 케이스. Risk 79, 상태 실행중. 대기업 납품계약은 확보했지만 양산 설비 리스 비용이 매출채권 회수보다 먼저 발생해 현금흐름이 얇고, 기술신용평가·특허·인증·납품계약서 근거가 흩어져 있어 심사가 지연될 위험이 있다. (원문: [[전라도-실감-페르소나-확장#JBG-205 — 오태림]])

**왜 이 케이스가 딥 히어로인가**
- **LLM 텍스트생성 가치가 가장 선명하다.** F2·F3(문서희·송민재)는 실 공공API 신호가 주인공이고 LLM은 보조지만, F1은 반대다 — 흩어진 기술신용 근거(특허·인증·납품계약)를 하나의 품의 초안으로 "패키징"하는 작업 자체가 텍스트 생성 문제이고, 이건 규칙 기반 파이프라인으로는 흉내만 낼 수 있는 지점이다. 여기서만 "AI가 실제로 문장을 쓴다"를 증명할 수 있다.
- **Zero-PII 티어 라우팅 시연에 최적이다.** 대표 개인보증·매출채권·계약 원문처럼 반출 위험이 높은 필드가 많은 도메인이라, "원문은 안 나가고 최소필드만 프런티어로 간다"는 서사가 억지스럽지 않고 케이스 성격과 맞아떨어진다.
- **검증 에이전트가 자기 오류를 잡는 장면을 자연스럽게 연출할 수 있다.** 계약일·금액·서류 누락 여부처럼 "숫자가 틀리면 바로 티가 나는" 필드가 많아, 준법 검토 에이전트의 재검이 시연에서 설득력을 갖는다.

## 2. 엔드투엔드 스텝

각 스텝: (a) 화면(S-ID·링크) (b) 데이터 (c) 실동작 등급 — 🟢 실동작 / 🟡 파이프라인 실·출력 목업(정직 라벨) / ⚪ 목업.

| # | 스텝 | (a) 화면 | (b) 데이터 | (c) 등급 |
|---|---|---|---|---|
| 1 | **신호 인입** — 수주잔고 증가 + 원자재·설비 리스 지출 선행, 대표 개인보증 의존도 상승 신호가 잡힌다 | [[01-메인-업무보드]] `S-03` 신규접수(`received`) 컬럼에 케이스 카드 진입 | `Signal[]`(합성): 매출채권 회수 지연폭, 설비 리스 견적액, 납품계약 잔고 | 🟡 신호 자체는 목업 시드(실 여신시스템 연동 없음), 케이스 상태 전이 로직은 🟢 실 UI |
| 2 | **케이스 생성** — `S-03`에서 오태림 케이스가 `자료수집`→`AI검토` 컬럼으로 이동 | [[01-메인-업무보드]] `KanbanColumn`, [[02-케이스-상세]] `S-03-detail` `InfoGrid` | `Case`(오태림, JBWC 워크스페이스, riskLevel: high, docsStatus: 서류 일부 누락) | 🟢 실 UI(칸반 상태 전이) |
| 3 | **에이전트 판단** — 상환위험 분류 에이전트가 납품계약·매출채권·장비 견적의 일관성을 판단, 계약 체크리스트 에이전트가 기술신용 보강 체크리스트 생성 | [[02-케이스-상세]] `signals`/`evidence` 탭, [[03-에이전트-실행뷰]] `AgentRunCard`(재무요약→상환능력체크 순서) | `ccl_review_notes`, `ccl_doc_checks`, `ai_recommendations` | 🟡 분류·추출 로직은 파이프라인 실재, 출력 텍스트는 저비용/로컬 티어 실 LLM(§3) |
| 4 | **실 LLM 품의/회신 초안 생성** — RM 보좌 에이전트가 설비리스 조건안·운전자금 연결안·추가서류 요청안을 생성. **여기가 실 LLM 지점.** | [[02-케이스-상세]] `evidence` 탭 `RecommendationDraftPanel`, [[03-에이전트-실행뷰]] `AgentRunCard`(품의초안 에이전트, `EnterToRunHint`) | `RecommendationDraft.actionDraft`(프런티어 생성), Zero-PII 최소필드 입력(§3) | 🟢 **실 LLM**(claude/codex `/llm` 게이트웨이) |
| 5 | **검증 에이전트** — 준법 검토 에이전트가 계약일·금액·서류 누락 여부를 재검, 초안의 인용 오류를 스스로 잡아 `needsReview`로 되돌림 | [[02-케이스-상세]] `verify` 탭 `PolicyCitationBox`/`GuardrailResult`, [[03-에이전트-실행뷰]] `RunStatusDot: needsReview` | 검증 결과(합/불), 반려 사유 | 🟡 검증 판정 로직은 실재(harnessGuardCheckAssertions류), 재검 서술은 실 LLM 출력 재검 |
| 6 | **가드레일(PII 차단)** — 원계약 원문·대표 개인정보가 반출 페이로드에 섞이면 즉시 차단 | [[02-케이스-상세]] `verify` 탭 `PiiScanResult` | `harnessGuardCheckPII()` 판정 결과 | 🟢 실동작(정규식 기반 PII 패턴 탐지, `harnessCore.js`) |
| 7 | **사람 승인 게이트** — L3 승인(여신 품의) 요청이 뜨고, 승인자가 초안·근거·규정검증을 보고 승인/거부/수정후승인 결정 | [[04-승인-대기·결정]] `S-04` → `S-04-detail`(`ActionDraft`/`EvidencePolicy`/`GateDecision` 3분할) | `ApprovalRecord`(requesterId: rm-copilot-agent, level: L3, gateChecks) | 🟢 실 UI(승인 결정 자체는 사람, 결정 이전 상태까지는 실동작) |
| 8 | **감사 봉인** — 결정 확정 시 `CCL_APPROVAL_DECIDED` 감사 이벤트 기록 | [[02-케이스-상세]] `audit` 탭 `AuditMiniTimeline`, [[05-통계-추적-패널]] 감사 실효성 뷰 | `ccl_audit_logs`(actor, action, target, evidenceId) | 🟢 구조화 로그는 실동작 / 🟡 해시체인 무결성 검증 UI는 예선 MVP 한정(§4) |

## 3. 실 LLM 포인트 상세

**어디서 실제로 도는가**: 스텝 4(품의/회신 초안 생성)가 유일한 실 LLM 지점이다. `/llm` 게이트웨이(`api-proxy.mjs` `handleLlm`)가 claude↔codex CLI를 라우팅한다(claude 2.1.198 · codex 0.142.5, paperclip式).

**티어 라우팅**
- **분류·추출(로컬/저비용 티어)**: 계약서 필드 추출, 서류 체크리스트 매칭, 매출채권·리스 견적 일관성 판정 — 짧은 구조화 출력, 반복 호출량이 많은 구간.
- **판단·초안(프런티어 티어)**: 기술신용 근거 패키징 문장, 설비리스 조건안, RM 회신 초안 — 서술 품질이 승부처인 구간만 claude/codex 프런티어로 올린다.
- 원가 근거: 실측 1회 호출 in 49K(대부분 캐시)·out 3토큰·$0.115, break-even 대비 10배 여유([[Q13-토큰비용-유닛이코노믹스|Q13]]).

**Zero-PII 최소필드 페이로드 예시(합성)** — 프런티어로 나가는 실제 필드는 이 정도로 제한한다:

```json
{
  "caseId": "JBWC-CCL-0205",
  "riskLevel": "high",
  "affiliate": "jb-woori-capital",
  "sector": "산업용 센서 제조",
  "revenueTrend": "수주잔고 증가·입금 지연",
  "techCreditGrade": "T-3 (미확정)",
  "contractAmountBand": "3억~5억",
  "leaseAmountBand": "1억~2억",
  "guaranteeDependency": "high",
  "docsGap": ["기술신용평가서", "납품계약 부속서류"]
}
```
회사명·대표 성명·사업자등록번호·계약 원문·계좌번호는 이 페이로드에 없다 — `harnessGuardCheckPII()`가 반출 직전 이 계약을 강제한다.

**폴백 사다리 시연법** — [[Q14-오류로깅-폴백사다리|Q14]] 그대로 재현:
1. 정상 호출 — claude가 품의초안을 생성.
2. `timeoutMs`를 인위적으로 낮춰(예: `1`) claude 호출을 강제 실패시킨다 → 반대 엔진 codex로 1회 폴백 재시도.
3. codex도 실패하도록 조작하면 `escalated: true, queue: "human"` 응답 → [[04-승인-대기·결정]] 승인 큐로 격상되어 사람 앞에 도착한다("AI가 조용히 사라지지 않는다"는 서사의 실물 증거).
4. 모든 시도(성공·실패 포함)가 `llm-runs.jsonl`에 한 줄씩 남는다(`runId, engine, tier, errorClass, retry, fallbackPath`).

**검증 에이전트가 자기 오류를 잡는 장면** — 품의초안이 계약일 또는 금액을 잘못 인용하면, 준법 검토 에이전트가 `PolicyCitationBox`/근거 대조에서 불일치를 발견해 해당 `AgentRunCard`를 `needsReview`로 되돌린다. 이 되돌림 자체가 [[03-에이전트-실행뷰]]의 `Shift+Enter`(반려/재검토, 사유 필수) 경로를 그대로 탄다 — 새 UI가 필요 없다.

## 4. 가드레일·승인·감사

- **PII 반출 차단**: `harnessGuardCheckPII()`(`harnessCore.js:74-110`, 정규식 기반 주민번호/전화번호/11자리 이상 숫자열 탐지) — `beforeCaseCreate`/`afterAgentRun`/`beforeCustomerMessage` 훅에서 강제. 위반 시 값이 아니라 "어떤 종류가 걸렸는지"만 표시(원문 미노출).
- **고위험 자동종결 차단**: `harnessGuardCheckAutoClose()` — riskLevel high/critical 케이스는 에이전트가 스스로 종결 상태로 못 넘어간다. 오태림 케이스(Risk 79 = high)는 이 가드에 항상 걸린다.
- **사람 승인(수정후승인)**: [[04-승인-대기·결정]] `S-04-detail` 3분할 — 설비리스 조건이 초안과 다르게 조정될 여지가 크므로, 이 케이스의 대표 데모 결정 경로는 "승인"이 아니라 **수정후승인**(`DraftEditor` 편집 → `DiffPreview` → 승인)으로 잡는다. L3이므로 Enter 단독 종료 금지(2단 확인 강제).
- **감사 봉인 — 정직 표기**: 감사 이벤트 자체(구조화 로그, `ccl_audit_logs`: actor/action/target/evidenceId)는 JB_project2에도 실재한다. **다만 해시체인 무결성 검증 UI(`prevHash→hash` 검증, `verifyAuditChain()`, `exportAuditJson()`)는 예선 MVP(`02_제품/app/`) 한정 구현**이며 JB_project2(제출 정본)에는 아직 이식되지 않았다 — F1 시연에서 감사 탭을 열 때 "해시체인 검증"까지 보여주려면 [[05-통계-추적-패널]]의 예선 MVP 화면을 별도로 인용해야 하고, JBWC 콘솔 화면만으로는 로그 존재까지만 보여줄 수 있다. 과장 금지.

## 5. 데모 스크립트(구두, 30~60초)

> 화면 전환 큐는 `→`로 표시. 대사는 그대로 읽어도 되는 구어체.

1. `[S-03 업무보드, JBWC 워크스페이스]` "군산에서 센서를 만드는 스타트업, 오태림 대표 케이스입니다. 대기업 납품은 잡았는데, 설비 리스비가 매출채권보다 먼저 나가는 상황이에요." → `S-03-detail` 진입
2. `[S-03-detail evidence 탭]` "기술신용 근거가 흩어져 있죠. 특허, 인증, 납품계약서 — 이걸 사람이 일일이 묶으면 반나절입니다." → `runs` 탭으로 전환
3. `[S-05 에이전트 실행뷰, 품의초안 에이전트 카드 Enter]` "지금 Enter를 누르면 실제로 LLM이 돕니다. 목업이 아니에요 — claude 게이트웨이를 통해 이 카드가 실행됩니다." → 로딩 스피너(퍼센트 없음)
4. `[결과 도착, AgentRunCard 완료]` "품의 초안이 나왔습니다. 그런데 회사명도, 대표님 성함도, 계약 원문도 밖으로 안 나갔어요 — 프런티어에는 이 최소 필드만 갔습니다." (Zero-PII 페이로드 예시 화면 오버레이)
5. `[verify 탭]` "준법 검토 에이전트가 계약일을 다시 확인합니다 — 방금 자기가 만든 초안의 오류를 스스로 잡는 겁니다." → `needsReview` 배지
6. `[타임아웃 조작 데모, 선택]` "일부러 응답을 지연시켜 볼게요. claude가 실패하면 codex로 넘어가고, 둘 다 실패하면 — 조용히 사라지는 게 아니라 사람 큐로 올라옵니다." → escalated 배지 → `S-04`
7. `[S-04-detail 3분할]` "여기서 사람이 결정합니다. 승인, 거부, 아니면 수정 후 승인. AI는 초안까지만, 최종은 항상 사람입니다." → 수정후승인 클릭 → 2단 확인
8. `[감사 탭]` "결정 하나하나가 감사 로그에 남습니다." (해시체인 검증까지 보여줄 경우: "이 검증은 지금 예선 프로토타입에서 실측 구현돼 있고, 본선 콘솔에도 곧 옮깁니다" — 정직 표기 유지)

## 6. 수용 기준(빌드 완료 판정)

- [ ] JBWC 콘솔에 오태림 케이스(가칭 `JBWC-CCL-0205`)가 시드로 존재하고, [[01-메인-업무보드]] 칸반 6컬럼 중 하나 이상에서 실제로 렌더된다.
- [ ] [[02-케이스-상세]] `evidence`/`runs`/`verify`/`audit` 탭 전부가 오태림 케이스 데이터로 진입 가능하다(빈 상태 아님).
- [ ] [[03-에이전트-실행뷰]] 품의초안 에이전트 카드에서 Enter 실행 시 `/llm` 게이트웨이를 통해 **실제 claude 또는 codex 응답**이 도착한다(하드코딩 텍스트 아님).
- [ ] Zero-PII 페이로드(§3)가 실제 네트워크 요청 바디에서 확인 가능하고, 회사명·대표 성명·계약 원문이 그 안에 없다.
- [ ] `timeoutMs` 조작으로 폴백 사다리(claude→codex→escalated)가 최소 1회 재현 가능하다.
- [ ] 준법 검토 에이전트의 `needsReview` 되돌림이 최소 1개 시나리오에서 재현 가능하다(사전에 오류를 심은 초안이든, 실제 LLM이 만든 오류든).
- [ ] `harnessGuardCheckPII()`가 오태림 케이스 컨텍스트에서도 원문 반출 시도를 차단하는지 1회 확인됨.
- [ ] [[04-승인-대기·결정]] `S-04-detail`에서 수정후승인 경로(DraftEditor → DiffPreview → 2단 확인)가 오태림 케이스로 완주된다.
- [ ] 감사 탭에 `CASE_CREATED`~`CCL_APPROVAL_DECIDED` 최소 4개 이벤트가 순서대로 남는다.
- [ ] §5 데모 스크립트를 실제 화면으로 완주 시 60초 이내(리허설 기준).

## 7. 의존성

- **Gate 0 — `/llm` 게이트웨이 JB_project2 이식**: 현재 `handleLlm`/`llm-runs.jsonl`/`liveLlmBlock`은 예선 `02_제품`에만 있다. F1의 실 LLM 지점(스텝 4)은 이 게이트웨이가 JB_project2(제출 정본)에 이식돼야 성립한다 — 미이식 상태에서는 F1 전체가 🟡로 강등된다.
- **PR#1 머지**: `feat/live-agent-ccl-financial`(River-181 fork, `LSB-afk/JB_project2#1`)이 이미 `CCL-0001`(구 히어로 전주카페) 케이스를 대상으로 실 LLM 슬라이스(claude/codex 라우팅 + `harnessGuardCheckPII`/`Assertions` 재검)를 구축해 놓았다. **리스크**: 이 파이프라인은 케이스 ID·데이터가 옛 히어로(전주카페)에 묶여 있다 — 오태림(JBG-205)으로 갈아타려면 (a) PR#1을 먼저 머지하고 (b) 같은 파이프라인을 오태림 케이스 데이터로 재시드하는 별도 작업이 필요하다. 파이프라인 자체를 다시 만들 필요는 없다.
- **JBWC 워크스페이스 시드 정합**: 오태림은 JB우리캐피탈 콘솔(`jb-woori-capital` 워크스페이스, `jbWooriCapital*`/`wooricap*`) 소속이다. 기존 실 LLM 슬라이스가 은행 측 `ccl-financial`(기업여신) 롤에 붙어 있다면, JBWC 콘솔로 옮기거나 최소한 워크스페이스 간 케이스 라우팅을 확인해야 한다 — 계열사 정합 확인이 안 되면 스텝 2(케이스 생성)부터 어긋난다.
- **감사 해시체인 이식 여부**: §4에서 정직 표기한 대로, 해시체인 검증 UI가 JB_project2에 없다면 F1 데모의 감사 탭은 "로그 존재"까지만 보여줄 수 있고 "무결성 검증" 시연은 예선 MVP로 대체해야 한다 — 팀이 이식할지 정직 표기로 갈지 결정 필요.

## 연결

- [[_쇼케이스-빌드플랜]] · [[전라도-실감-페르소나-확장]]
- [[01-메인-업무보드]] · [[02-케이스-상세]] · [[03-에이전트-실행뷰]] · [[04-승인-대기·결정]] · [[05-통계-추적-패널]]
- [[Q13-토큰비용-유닛이코노믹스]] · [[Q14-오류로깅-폴백사다리]]
