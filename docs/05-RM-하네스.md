# 05 · RM(관계관리 담당자) 역할 하네스

> 역할 하네스는 화면 필터가 아니라 독립 업무 하네스다. 이 문서는 RM 역할 하네스(scope `rm-officer`)의
> 제품·UX·에이전트·MD 산출물 설계를 정리한다.
> 기준 구현: `jeonse-protection` / 두 번째 선례: `corporate-credit`.
> 상호 참조(볼트 산출물): `08_본선/03_제품/07_역할-하네스/_HARNESS-RM.md` (커밋 dfc6ffb),
> 실행뷰 설계도 `08_본선/03_제품/설계도/03-에이전트-실행뷰.md`.

---

## 1. 제품 한 줄 설명

RM이 담당 고객의 여신 상담 케이스를 **"급한 순 근거"로 모아 보고**, 케이스마다 필요한 AI 에이전트를
**키보드 Enter 한 번으로 승인·실행**해 개별/통합 **MD 리포트**로 검토하는 내부 업무 지원 콘솔.
(실제 승인·금리·한도·신용평가·정책자금 대상 확정은 하지 않는다 — 모든 산출물은 내부 업무 참고용.)

## 2. 핵심 사용자 여정

1. 역할 레일에서 **RM** 진입 → 업무보드가 급한 순으로 케이스를 정렬해 보여준다.
2. 카운트 헤더(작업 전/중/후)와 필터(전체·작업 전·중·후)로 오늘 처리할 일을 좁힌다.
3. **숫자키로 케이스를 선택** → SUB에 케이스 헤더 + 상태 칩 + **에이전트 승인 큐**가 열린다.
4. 승인 큐에서 현재 선택된 에이전트에 **Enter로 승인·실행** → (모의) 실행이 개별 `.md` 산출물을 만든다.
5. 배정 에이전트를 모두 실행하면 **통합본 `.md`**가 자동 생성된다(개별 md를 옵시디언식 링크로 연결).
6. LLM 지연은 "다른 케이스 보기"로 흡수 — 실행 중에도 보드로 돌아가거나 다른 케이스에 진입할 수 있다.
7. 완료/고위험 케이스는 **승인 라우팅**으로 넘어가 사람 검토를 받는다.

## 3. 화면별 UX 구성 (Figma SSOT 3장면)

- **화면 A · 업무보드**: 좌측 조직 레일 + 전용 사이드바("RM 업무지원 포털"), 우측 필터
  (`전체` / `처리해야할 작업 전`(기본) / `처리해야할 작업 중` / `처리해야할 작업 후`),
  배너 "RM님 업무 급한 순으로 모아왔어요" + 힌트 "키보드 숫자를 눌러 Case를 확인하세요",
  카운트 헤더(진행 전/중/후 개수), 케이스 카드 가로 리스트(상태 배지·고객·케이스번호·지역·은행·
  주제·상황·우선순위 근거·필요 에이전트 칩).
  - **SUB**: 헤더 카드(Case·날짜·위험등급·제목·상황·`고객 정보` 버튼) + 상태 칩(작업 전/중/후) +
    **에이전트 승인 큐 카드**(에이전트·소속·산출물 `.md`·소요 N분 / 사용 이유 / 예상 기대값 /
    데이터 칩 / 우하단 "Enter를 눌러 승인해주세요"). 실행 중 카드는 딤 오버레이 + "조금만 기다려주세요"
    + **불확정(indeterminate) 스피너**.
- **화면 B · 진행 상태 + 통합 MD 뷰어**: 케이스 배지가 `작업 완료`로 전환, 다른 케이스는 `분석중`.
  뷰어 탭 `[통합본 | 개별 에이전트.md ×N]` + 문서 본문 + 접이식 섹션
  `[사용 에이전트/스킬 표(에이전트·산출물.md·사용 데이터·날짜·관여율) | 사용 출처(링크)]`.
- **화면 C · 문서 모달**: 보드 딤 처리 + MD 플로팅 모달(메타데이터 블록 → 제목 → Summary → 근거 표).
  통합본 안에서 개별 md로 링크 이동(옵시디언식).

## 4. 주요 컴포넌트 목록

- 업무보드: `rmoBoardView` / 카운트 헤더 `rmoCountHeader` / 필터 `rmoBoardFilters` / 케이스 카드 `rmoBoardCard`
- 케이스 SUB: `rmoCaseSubSection` / 승인 큐 `rmoAgentQueueSection` / 고객 정보 `rmoCaseDetailMarkup`
- MD 뷰어: `rmoDeliverableViewerSection` / 문서 모달 `rmoDocumentModalMarkup` / 렌더러 `rmoRenderMarkdown`
- 접수 위저드: `rmoCaseCreationView` / 우선순위 미리보기 `rmoWizardPreviewMarkup`
- 하네스: `rmoHarnessView` / 라우터 `rmOfficerHarnessPage` / 사이드바 점유 `rmoTakeoverSidebar`
- 서비스: `getRmOfficerSidebarCounts` · `searchRmOfficerRecords` · `createRmOfficerCase` ·
  `approveRmOfficerAssignment` · `recordRmOfficerAgentRun` · `rmoDecideApproval`
- 도메인 로직: `computeRmOfficerPriority`(우선순위 근거) · `rmoBuildAgentDeliverable` / `rmoBuildIntegratedDeliverable`(MD)
- 스타일: `styles.css` 파일 끝 `/* === RM Officer Harness === */` 섹션(공통 `jbwc-*`/`jpo-*` 토큰 재사용)

## 5. 키보드 인터랙션 규칙

| 키 | 동작 |
| --- | --- |
| 숫자 `1`~`9` | 업무보드에서 해당 순번 케이스 선택(급한 순 정렬 기준) |
| `방향키` / `j` `k` | 승인 큐에서 현재 선택 에이전트 이동 |
| `Enter` | 현재 선택 에이전트 승인·실행 |
| `Esc` | 문서 모달 닫기 / 케이스 선택 해제 후 보드 복귀 |

- **Enter 하나에 모든 책임을 주지 않도록** 현재 선택 상태(케이스·에이전트)를 항상 강조 표시한다.
- 입력 필드(input/textarea/select) 포커스 중에는 단축키를 가로채지 않는다. 마우스 클릭도 모두 지원한다.
- 승인하지 않을 에이전트는 그대로 둘 수 있다. 실행 중=로딩, 완료=딤/체크로 상태 변화를 표시한다.

## 6. 에이전트 워크플로우

요청 → **RM Case Triage Orchestrator**(우선순위 근거 산정·에이전트 배정) → 상담 도메인 에이전트
(Marine Risk / Credit Care / Salary Flow / DSR Guard / Youth Finance / Policy Finance) →
**Action Agent**(다음 조치 태스크) / **Customer Guidance Draft Agent**(안내 문안 초안·발송 승인 대기) →
**Compliance Guardrail Evaluator Agent**(금지 표현·PII·high/critical 자동 완료 차단) →
**Approval Routing Agent**(사람 승인 큐) → 담당자 검토 → Audit/State.

- 에이전트는 계층적으로(에이전트 → 스킬 → 데이터/출처) 차별성이 드러나되 기술적으로 과하게 보이지 않게 구성.
- 실행은 **시뮬레이션**(모의 실행 + `rm_officer_agent_runs`/`rm_officer_agent_handoffs` 기록)이며,
  선택적으로 로컬 Ollama 모델을 붙일 수 있다(실패 시 graceful degrade + `needsReview` 기록).
- **high/critical 위험은 자동 완료하지 않고** `humanReview` + 승인 라우팅으로 강등된다(가드레일 불변식).

## 7. MD 결과물 구조

- **개별 에이전트 `.md`**: 프론트매터(case·agent·org·date·note) → `# 제목` → `## Summary` →
  `## 근거`(항목·내용·출처 표) → `## 예상 기대값` → 가드레일 인용문.
- **통합본 `.md`**: 상황 → 우선순위 근거 → **개별 산출물 링크(`[[파일명]]`)** →
  **사용 에이전트/스킬 표**(에이전트·산출물.md·사용 데이터·날짜·관여율) → **사용 출처** 목록.
- 뷰어/모달은 자체 생성 MD만 렌더하며 `escapeHtml`로 안전하게 표시한다. AI 산출물 시맨틱은
  **"내부 업무 참고용 · 담당자 검토 필요"**.

## 8. MVP 범위와 고도화 범위

- **MVP(현재)**: 업무보드(급한 순·근거)·키보드 승인 큐·개별/통합 MD·우선순위 근거·승인 라우팅·
  scope 강제 저장소·보안 훅(PII/자동 종결/승인)·자체 검증 루프·모의 실행 + 선택적 로컬 모델.
- **고도화**: 실데이터 커넥터(기상특보·급여주기·정책자금 요건) 연결, 에이전트 실행 병렬화·재시도,
  통합본 자동 요약 품질 강화, 콜백/알림 채널 연동, 승인 매트릭스(L1~L4) 정교화, RAG 근거 인용 링크 심화.

## 9. 해커톤 발표 차별점

1. **근거 기반 우선순위** — 단순 점수가 아니라 "왜 급한지" 문장과 데이터 출처를 함께 노출한다.
2. **키보드 퍼스트 승인 큐** — 숫자키 선택 → Enter 승인으로 케이스를 빠르게 처리(마우스 최소화).
3. **개별 → 통합 MD 파이프라인** — 에이전트별 산출물을 옵시디언식 링크로 엮은 통합 리포트로 검토 동선 단축.
4. **가드레일 내장** — 실제 금융 판단 금지·PII 차단·high/critical 자동 종결 금지를 코드/훅/검증기로 강제.
5. **그룹 확장성 증명** — 전세보호·기업여신·JB우리캐피탈에 이은 4번째 독립 역할 하네스로 표준 계약을 재입증.

---

### 각주

- **[조건부] Case 2 페르소나 정합차**: Figma는 `강하준 · JBG-206 · 광주 북구 · 전북은행`이지만,
  볼트 canon(`전라도-실감-페르소나-확장`, 7/3 갱신)은 `조동준 · 광주 광산구 · 광주은행`이다.
  팀 확정 전이므로 seed는 Figma 표기를 유지하되 페르소나 필드(이름·지역·은행)를
  `rmOfficer-db.js`의 `rmoPersonas` 한 곳에 모아 **한 줄 교체**로 canon 전환이 가능하게 했다.
  `verify_static.py` needle에는 페르소나 이름을 넣지 않았다(제목·버튼·에이전트명 등 안정 라벨만 검증).
- **[확정] 퍼센트 없는 로딩**: 실행뷰 설계도 §5에 따라 실행 중 표시는 "조금만 기다려주세요" 문구 +
  불확정 스피너로 구현하고 진행률 숫자(예: 40%)는 노출하지 않는다.
