# 전세사기 보호 담당자 역할 하네스 v2 (2026-07-03)

원칙: **역할 = 독립 업무 하네스**, 그리고 **MoveValue의 부동산 데이터 아이디어는 "가격 데이터 엔진"으로만 흡수** —
화면/보드/메뉴/에이전트/케이스 생명주기는 전세사기 보호 업무 전용으로 새로 설계했다.
AI는 위험 "신호"와 확인 항목만 제안하고, 전세사기 여부·법률·보증·피해자 결정은 항상 담당자가 판단한다.

- roleKey: `jeonse-protection` · route base: `#/roles/jeonse-protection` · activeView: `jeonse-protection-harness`
- 저장소: `jpo-ops-db-v2` (`jpoRepository` 5개 진입점, scope 미지정 시 `role scope is required` 예외)

## 1. 레이어 (표준 하네스 계층 유지)

config(메뉴 25종·유형 8·신호 10·보드 7컬럼) → agents registry(10종+Skills 9) → rules/hooks(9종) →
**jeonsePublicData.adapters.js**(공공데이터 엔진) → **jeonsePriceRisk.service.js**(신호 스코어링) →
db(v2 스키마+seed) → services(counts 26·검색·보강·생성·실행) → views(칸반 보드·25화면·5단계 위저드·하네스) →
commands(6) → sidebar(점유 중재) → router. manifest는 harnessRegistry.js에 등록, runHarnessSelfTest 6종 검증.

## 2. 케이스 lifecycle 보드

신규 접수 → 데이터 보강 중 → 위험 신호 검토 → 담당자 검토 필요 → 외부기관 연계 → 지원 안내 완료 → 보류/추가자료 요청.
카드: caseNo · 익명 고객 ID(CUST-JS-*) · 주택 유형 · 마스킹 주소("… ***") · 보증금 · 계약/만기일 · 위험도 ·
주요 위험 신호 · 담당자 · SLA · **데이터 연계 상태(sourceMode: live_api/snapshot/fallback/manualRequired)**.

## 3. 데이터 모델 (모든 row에 roleKey/workspaceId)

jeonse_cases(주택·계약·체크 상태·docChecklist·sourceMode) · jeonse_price_snapshots(중앙값·표본·전세가율·추정 공시가) ·
jeonse_risk_signals(signalType 10종) · jeonse_registry_checks · jeonse_guarantee_checks · jeonse_support_referrals
(victimApplication/legal/financeHousing/care) · jeonse_audit_logs(CASE_CREATED/DATA_FETCHED/RISK_UPDATED/
HUMAN_REVIEW_REQUIRED…) · jeonse_agent_runs · agent_handoffs · approvals · external_connectors(Seoul/MOLIT/HUG·등기 manual) ·
users/role_assignments/inspection_schedules.

## 4. 공공데이터 엔진

- 프록시(`scripts/api-proxy.mjs`, :8020): 국토부 실거래 7종(dataset별 전용 키 → MOLIT_SERVICE_KEY →
  MOLIT_API_KEY → PUBLIC_DATA_API_KEY → DATA_GO_KR_KEY 체인) + 서울 열린데이터 키 프로브
  (SEOUL_OPEN_API_KEY/SEOUL_API_KEY/MOVEVALUE_SEOUL_OPEN_API_KEY). **키는 환경변수로만.**
- adapter(`fetchJeonseMarketData`): ?live=1 + 프록시 정상 → `live_api`; 아니면 지역 스냅샷(`snapshot`);
  둘 다 없으면 `fallback` — 이때 위험도를 낮게 확정하지 않고 최소 medium + "담당자 확인 필요".
- 공시가격 API 미연동: 매매 중앙값 기반 "추정" 공시가격만 표기(확정값 아님).

## 5. 위험 스코어링 (computeJeonseRiskAssessment)

신호 10종: JEONSE_RATIO_HIGH · DEPOSIT_OVER_OFFICIAL_PRICE · ABOVE_NEIGHBORHOOD_MEDIAN ·
LOW_COMPARABLE_COUNT · REGISTRY_RIGHTS_UNKNOWN · GUARANTEE_STATUS_UNKNOWN ·
AUCTION_OR_FORECLOSURE_DEADLINE · LANDLORD_RISK_MANUAL_REQUIRED · ILLEGAL_BUILDING_MANUAL_REQUIRED ·
CONTRACT_DATE_URGENT. 규칙: 등기/보증 unknown ⇒ 최소 medium · 경공매 ⇒ high+(D-14 이내 critical) ·
high/critical ⇒ requiresHumanReview=true & 자동 완료 금지(훅이 needsReview로 강등) ·
피해자 결정/법률/보증 ⇒ "담당자 확인 필요"로만.

## 6. Seed (JEONSE-0001~0006)

화곡동 다세대 전세가율 91%(high) · 신림동 오피스텔 보증 확인 필요 · 개봉동 아파트 전월세 비교(보강 중) ·
인천 미추홀 다가구 경공매 D-9(critical·fallback 스냅샷) · 대전 반환 지연(high·법률/금융/심리 연계) ·
부산 계약 전 긴급(D-10) + 타 역할 격리 seed(JEONSE-OTHER-0001). 모든 메뉴 count ≥1 충족.

## 7. 검증

`npm run build`(47파일 — 프록시 env 계약·PII 패턴·단정 리터럴 포함) · `npm run test:e2e`(47) ·
`npm run test:smoke`(2). 전용 스위트가 §14 요구 전부 커버: 진입/메뉴, generic·무관 샘플 부재,
보드 lifecycle, fallback 표시, 접수 기록 체인, 고위험 human review, scope 격리, PII 차단, 모바일.
